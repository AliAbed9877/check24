{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <h1 class="text-center">Comparison of Cards</h1>
        </div>
        <a href="{{ path('app_cards') }}" class="btn btn-primary d-block my-4 col-2 mx-auto">Compare now</a>
    </div>
{% endblock %}